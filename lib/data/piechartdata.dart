/// `PieChartData` đại diện cho một mảnh trong biểu đồ tròn (tên và giá trị).
class PieChartData {
  final String name;
  final double value;

  const PieChartData({
    required this.name,
    required this.value,
  });
}
